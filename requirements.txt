# Python >= 3.10 (developed and verified with 3.12). CPU-only torch is sufficient.
# Install torch first: `pip install torch` (macOS / CUDA). Linux/Windows CPU-only machines may use
# the smaller wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3
numpy>=1.26,<3
pandas>=2.1,<4
scipy>=1.11,<2
scikit-learn>=1.3,<2
matplotlib>=3.8,<4
seaborn>=0.13,<0.14
PyYAML>=6.0,<7
tqdm>=4.66,<5
Markdown>=3.5,<4
tabulate>=0.9,<0.11
requests>=2.31,<3
# pretrained text encoder (embedding_model: sentence-transformers/all-MiniLM-L6-v2, ~90 MB from Hugging Face)
sentence-transformers>=5.7,<6
# notebook execution
jupyter>=1.0,<2
nbformat>=5.9,<6
nbclient>=0.9,<0.12
nbconvert>=7.10,<8
ipykernel>=6.25,<8
# development
ruff>=0.5,<1
pytest>=8,<9
